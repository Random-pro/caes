<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysUserMapper">

    <!-- 用作前台学生信息获取 -->
    <resultMap type="StudentInfo" id="StudentInfoResult">
        <id     property="userId"       column="user_id"      /><!-- 用户/学生ID -->
        <result property="userName"     column="user_name"    /><!-- 学生姓名 -->
        <result property="email"        column="email"        /><!-- 邮箱 -->
        <result property="phonenumber"  column="phonenumber"  /><!-- 手机号 -->
        <result property="sex"          column="sex"          /><!-- 性别 -->
        <!-- 民族、教育程度、学号等 -->
    </resultMap>

    <sql id="selectStudent">
        select user_id, user_name, email, phonenumber, sex from sys_user
    </sql>

    <!-- 根据user_id 获取学生信息 -->
    <select id="SelectStudentInfoById" parameterType="Long" resultMap="StudentInfoResult">
        <include refid="selectStudent"/>
        where user_id = #{userId}
    </select>
</mapper>